# ==========================================
# BudgetWise Application Configuration
# ==========================================

# Server
server.port=8082

# MongoDB Atlas
spring.data.mongodb.uri=mongodb+srv://harini:harini123@budgetwise-cluster.lr2y4ct.mongodb.net/budgetwise?retryWrites=true&w=majority&tls=true

# ==========================================
# JavaMailSender (Gmail SMTP)
# Set MAIL_USER and MAIL_PASS as environment variables,
# or replace ${MAIL_USER} / ${MAIL_PASS} directly with your values.
# ==========================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=chharini2004@gmail.com
spring.mail.password=gerjiajstsitbhce
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=3000
spring.mail.properties.mail.smtp.writetimeout=5000

# ==========================================
# JWT Configuration (secret must be 32+ characters)
# ==========================================
app.jwt.secret=BudgetWise$SecretKey#2024@Secure!JWT#Token$256Bit
app.jwt.expiration=86400000

# App Frontend URL (for email verification/reset links)
# If using VS Code Live Server use http://localhost:5500
# If opening HTML files directly (file://) keep this as-is for links
app.frontend.url=http://localhost:8082

# Logging
logging.level.com.budgetwise=DEBUG
